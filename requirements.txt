# Core dependencies
numpy
pandas
matplotlib
psutil
datasets
wandb

# PyTorch - automatically uses CPU, CUDA, or MPS based on system
torch
torchvision

# JAX - CPU version (safe default, works everywhere)
# For CUDA support on H100/other NVIDIA GPUs, install separately:
#   pip install "jax[cuda12]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
#   OR for CUDA 11: pip install "jax[cuda11_local]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
# For TPU support, install on TPU VM:
#   pip install "jax[tpu]" -f https://storage.googleapis.com/jax-releases/jax_tpu_releases.html
jax[cpu]
flax

# Optional: GPU monitoring tools (for energy measurement on NVIDIA GPUs)
# Install only if you have NVIDIA GPUs and want energy profiling:
#   uv pip install --system "nvidia-ml-py>=12.535.0"
# nvidia-ml-py>=12.535.0  # Provides the pynvml module

# Optional: datasets backend dependency (required for HF datasets + ImageNet loader)
pyarrow>=14.0.0,<15.0.0

# Optional: For better profiling and visualization
# tensorboard  # Uncomment if you want TensorBoard integration
